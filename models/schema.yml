version: 2

models:
  - name: customers
    description: One record per customer
    columns:
      - name: customer_id
        description: Primary key
        tests:
          - unique
          - not_null
      - name: first_order_date
        description: NULL when a customer has not yet placed an order.

<<<<<<< Updated upstream
  - name: orders
    description: Multiple order per customer
=======
  - name: stg_orders
    description: This model cleans up order data
>>>>>>> Stashed changes
    columns:
      - name: order_id
        description: Primary key
        tests:
          - unique
          - not_null
      - name: status
        description: Order status with accept values
        tests:
          - accepted_values:
              values: ['placed', 'shipped', 'completed', 'return_pending', 'returned']
      - name: customer_id
        description: ref to customer order's id
        tests:
          - not_null
          - relationships:
              to: ref('stg_customers')
              field: customer_id